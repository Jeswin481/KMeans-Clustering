# KMeans-Clustering
"This project uses the Iris dataset from scikit-learn and applies K-means clustering to uncover patterns in the data. The features are scaled using MinMaxScaler before clustering."
